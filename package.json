{
  "name": "87oui-wordpress-docker",
  "version": "1.0.0",
  "description": "coding-set",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "npm ci --prefix theme",
    "prepare": "husky install",
    "start": "npm start --prefix theme"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.2"
  }
}
